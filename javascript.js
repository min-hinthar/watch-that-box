const box = document.getElementById('box');

function grow() {
    box.setAttribute.("id", "growBox");
}
function blue() {
    box.setAttribute.("id", "blueBox");
}
function fade() {
    box.setAttribute("id", "fadeBox");
    setTimeout(funcion());
    }
}
function reset() {
    box.setAttribute("id", "resetBox")
}
